<!-- Main container -->
<nav class="level">
  <!-- Left side -->
  <div class="level-left">
    <%= render partial: "shared/pageLevelTitle", locals: { title: "Accounts" } %>
  </div>

  <!-- Right side -->
  <div class="level-right">
    <%= render partial: "shared/pageLevelNewButton", locals: { path: new_account_path } %>
  </div>
</nav>