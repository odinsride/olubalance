<!-- Main container -->
<nav class="level">
  <!-- Left side -->
  <div class="level-left">
    <%= render partial: "shared/pageLevelTitle", locals: { title: "Accounts" } %>
  </div>

  <!-- Right side -->
  <div class="level-right">
    <%= render partial: "shared/pageLevelNewButton" %>
  </div>
</nav>