<div class="p-4 flex justify-between items-center shadow border-t-8 border-green-800">
  <!--   left side -->
  <div class="flex items-center">
    <%= link_to root_path do %>
      <%= image_pack_tag 'media/images/logo.png', width: "175", alt: "olubalance logo" %>
    <% end %>
  </div>

  <!--   right side -->
  <div>
    <a href="#" class="inline-block py-2 px-4 mr-2 shadow rounded border border-green-600 hover:border-transparent text-green-700 hover:text-gray-100 hover:bg-green-800 font-semibold capitalize transition ease-in duration-150">Login</a>
    <a href="#" class="inline-block py-2 px-4 shadow rounded font-semibold text-gray-100 bg-green-700 hover:bg-green-800 transition ease-in duration-150">Sign up</a>
  </div>
</div>