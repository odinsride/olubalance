<%= turbo_stream_action_tag(
  "append",
  target: "transactions",
  template: %(#{render @transactions}) 
) %>
<%= turbo_stream_action_tag(
  "replace",
  target: "pager",
  template: %(#{render "pager", pagy: @pagy})
) %>