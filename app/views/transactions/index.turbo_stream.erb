<%= turbo_stream_action_tag(
  "append",
  target: "transactionTbody",
  template: %(#{render "transactions/components/transactionLine", collection: @transactions, as: :transaction}) 
) %>
<%= turbo_stream_action_tag(
  "replace",
  target: "pager",
  template: %(#{render "transactions/components/pagination"})
) %>